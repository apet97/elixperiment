# Implemented threat-model closure

> [!NOTE]
> This is a phase-by-phase closure record. References to later release or image
> work describe the state at the time of that section. The current offline
> candidate gate includes release, container, advisory, and secret scans.

This file closes plan Section 27 against executable proof. The contract
transcription remains `docs/contract/threat_model.md`. A change to the threat
list still requires a new ADR.

**Reviewed parent:** `acc514e5009c75b55ec83457ec5d041541e45ab8`
(`docs: ledger P13-T06 complete`). This file is part of the P13-T07 change
on top of that parent. The implementing commit SHA is recorded in
the [historical implementation ledger](../archive/implementation-ledger.md)
after the feature commit.

Columns below add what P1-T04 did not yet have: code owner, exact test names,
last passing evidence, and residual risk. No high or critical row is closed by
manual review alone.

| Threat | Boundary | Mitigation | Code owner | Automated proof | Last passing evidence | Residual risk |
|---|---|---|---|---|---|---|
| Cross-workspace access | Context/query | Trusted `Scope` and compound `(installation_id, id)` lookups; foreign ids are `Policy.not_found/0` | `PumbleAutomation.Scope`, `PumbleAutomation.Installations.Policy` | `test/security/tenant_isolation_test.exs` (`workspace A ids under B are not_found`, `foreign resource routes do not leak existence`, `swapping installation_id cannot claim or decide another tenant's rows`, `no web module names PumbleAutomation.Repo`); `test/security/release_gate_test.exs` (`a job for another tenant cannot claim that tenant's execution`) | P13-T01 `3a1cac1`; re-run in P13-T07 `mix test test/security`; P15-T05 `release_gate_test.exs` | Occupancy parking (P7-T09) is unchanged and is not a tenancy hole. |
| OAuth CSRF | OAuth callback | One-time hashed state; atomic consume; local return paths only | `PumbleAutomation.Installations.OauthStates` (`lib/pumble_automation/installations/oauth_states.ex`) | `test/pumble_automation/installations/oauth_state_test.exs` (`returns the row once and refuses the same token afterwards`, `exactly one of many concurrent consumers wins`, `an expired state is refused and is not consumed`, `refuses an absolute URL as a return destination`); `test/security/web_security_test.exs` (`OAuth install refuses an off-origin return`); `test/security/release_gate_test.exs` (`an unknown OAuth state never reaches token exchange`) | P3-T03 `30f4945`; P13-T05 `7ed72d4`; re-run in P13-T07; P15-T05 | None for v1. Rate limits on the callback path are P13-T02 and already enforced. |
| Forged callback | Pumble ingress | Raw-body HMAC-SHA256 and constant-time compare; missing secret fails closed | `PumbleAutomation.Pumble.Signature` (`lib/pumble_automation/pumble/signature.ex`); `PumbleAutomationWeb.Plugs.VerifyPumbleSignature` | `test/pumble_automation/pumble/signature_test.exs` (`refuses the stored invalid example`, `refuses a body changed only in whitespace`); `test/pumble_automation_web/plugs/verify_pumble_signature_test.exs` (`a wrong signature is refused`, `a missing signature header is refused`, `an absent signing secret refuses every callback`); `test/security/release_gate_test.exs` (`a forged callback is 401 and the canary never reaches the log`) | P4 signature suite; re-run in P13-T07; P15-T05 | Invalid signatures remain 401 by documented divergence from the vendor SDK (403). Timestamp unit is still probe PR-03 and is not required for HMAC correctness. |
| Replay | Ingress | Durable dedupe key; webhook `Idempotency-Key`; no exactly-once claim | `PumbleAutomation.Ingress.Deduplication`; `PumbleAutomation.Ingress.Service` | `test/pumble_automation/ingress/deduplication_test.exs` (`exactly one writer wins a concurrent duplicate insert`); `test/pumble_automation/ingress/pumble_ingestion_test.exs` (`a second callback for the same event creates nothing`); `test/pumble_automation_web/controllers/inbound_webhook_controller_test.exs` (`a repeated Idempotency-Key returns the same receipt`); `test/security/release_gate_test.exs` (`a replayed signed event stores one receipt`) | P8-T02 `8243254`; P8-T04 `ac9eec2`; P8-T07 `e5a718d`; re-run in P13-T07; P15-T05 | Fallback I-9 + 900s bucket can split a duplicate across an epoch boundary (documented). Delivery ids stay on PR-01. |
| Token leak | Storage/logging | AES-256-GCM envelopes; write-only secrets; Phoenix `filter_parameters`; audit metadata allowlist | `PumbleAutomation.Crypto.Vault`; `PumbleAutomation.Connections`; `PumbleAutomation.Audit.AuditEvent` | `test/pumble_automation/crypto/vault_test.exs` (`never writes the plaintext into the envelope`, `no plaintext reaches inspect or the log`); `test/security/web_security_test.exs` (`a planted authorization code does not reach a debug log line`); `test/pumble_automation/audit/audit_test.exs` (`secret-shaped keys are refused and never stored`); `test/security/http_action_adversarial_test.exs` (`captured logs and persisted attempt rows never contain the planted secret`); `test/security/release_gate_test.exs` (unique canary absent; uninstall ciphertext SQL NULL) | P3-T02 `8f52cda`; P13-T05 `7ed72d4`; P13-T06 `2f4f51f`; gitleaks in `docs/security/review_results.md`; P15-T05 | Unstructured HTTP body excerpts can echo a remote-returned secret (`docs/security/http_action_review.md`). Structured log allowlist completes in P14-T01. Local `secret_key_base` placeholders and the test CA are documented gitleaks false positives, not production credentials. |
| SSRF | HTTP action | Resolve, reject, pin, revalidate redirects; `verify_peer`; no Req on this path | `PumbleAutomation.Connections.SafeHttp.Transport` (`lib/pumble_automation/connections/safe_http/transport.ex`); `PumbleAutomation.Connections.UrlPolicy` | `test/security/http_action_adversarial_test.exs` (blocked IPv4/IPv6/names/tricks, mixed DNS, pin-vs-rebind, redirect rebind, Host/SNI, auth strip, gzip bomb, Mint `verify_peer` source contract); `test/security/release_gate_test.exs` (`a private-network HTTP target is refused before a socket opens`) | P10-T05 `3219fbc`; re-run in P13-T07; P15-T05 | Named residuals in `docs/security/http_action_review.md` (excerpt echo, `%0d` in path, production DNS, no netns proof). Re-read in P15-T05; none upgraded. `Connections.test_connection/2` remains a typed refusal (product leftover, not an SSRF hole). Pumble API stays on Req against a fixed host (ADR-0010). |
| Workflow bomb | Compiler/runtime | Section 31 catalog, hard caps, typed runtime overrides | `PumbleAutomation.Limits`; `PumbleAutomation.Workflows.Validator` | `test/security/limits_test.exs` (`Section 31 defaults and hard caps are the documented numbers`, `node, depth, definition, and template owners accept the limit and refuse limit+1`, `a later step that would overflow context fails the run as resource_limit`); `test/security/release_gate_test.exs` (`an oversized webhook body is refused`) | P13-T02 `c247eef`; re-run in P13-T07; P15-T05 | Occupancy parks excess executions (P7-T09) instead of rejecting them. That is a queueing policy, not a missing cap. |
| Recursion loop | Pumble events | Own-bot filter (`aId == bot_user_id`); HMAC webhook lineage; depth 3 / descendants 8 | `PumbleAutomation.Executions.Lineage`; `PumbleAutomation.Ingress.TriggerMatcher` | `test/security/loop_prevention_test.exs` (`default event triggers ignore the installation bot`, `a child of a different workflow is admitted and returning to A is a loop`, `depth four is refused before any derived write`, `a verified lineage header propagates parent depth; a forged header does not`); `test/security/release_gate_test.exs` (`lineage depth four is refused before a derived write`) | P13-T03 `1d71a3d`; re-run in P13-T07; P15-T05 | Include-bot is a documented warning, not a block. Pumble events always root. |
| Job flood | Ingress/schedules | Workspace quotas and fail-closed ETS rate limits | `PumbleAutomation.Limits`; `PumbleAutomation.RateLimiter` | `test/security/limits_test.exs` (`queued executions accept the limit and refuse the next distinct create`, `callback failures skip HMAC once the catalog rate is reached`, `concurrent webhook and manual checks honour the per-key limit`, `restarting the limiter empties counts and fails closed while it is down`); `test/security/release_gate_test.exs` (`webhook brute-force guesses are unauthorized and create no execution`) | P13-T02 `c247eef`; re-run in P13-T07; P15-T05 | Per-node ETS is not cluster-global. Multi-node deployments share Postgres quotas for durable creates; HTTP flood capacity scales with node count until P16 sizing. |
| Approval spoof | Interaction | Actor policy, HMAC-bound one-time token stored as digest, constant-time compare | `PumbleAutomation.Executions.ApprovalService` (`lib/pumble_automation/executions/approval_service.ex`) | `test/pumble_automation/executions/approval_decision_test.exs` (`a second identical click is a no-op`, `a workspace member who is not an approver cannot decide`, `a callback actor from another workspace cannot decide`); `test/pumble_automation/executions/approval_request_test.exs` (`the database stores a digest, not the button payload`, `a payload minted for one approval cannot authorize another`, `role and group selectors are refused`); `test/security/release_gate_test.exs` (`an unauthorized actor and a tampered button cannot decide an approval`) | P11-T05 `5f670fb`; P11-T06 `eac20a1`; re-run in P13-T07; P15-T05 | Message update after commit is best-effort. A failed update does not reopen the decision. |
| Scope escalation | Pumble client | Explicit required-scope matrix; known omissions from the recorded request block locally; provider 403 is permanent; unverified mappings warn | `PumbleAutomation.Pumble.Scopes`; `PumbleAutomation.Workflows.Dependencies`; `PumbleAutomation.Workflows.Activation` | `test/pumble_automation/workflows/dependencies_test.exs` (`a permission omitted from the recorded request blocks`, `an unproven permission only warns`); `test/pumble_automation/workflows/activation_test.exs` (`a scope omitted from the recorded install request writes nothing and returns the full issue list`); `test/pumble_automation/pumble/client/error_test.exs` (`403 with a mapped scope is a missing scope`) | P6-T04 `49612eb`; P6-T05 `1bcf414`; re-run in P13-T07 | The snapshot records what the application requested; presence never proves a provider grant. Pumble remains the authorization boundary. Unverified operations (probe PR-07) never gate activation. |
| Log leakage | Observability | Field allowlist and redaction before JSON; credential filters; callback `log: false` | `PumbleAutomation.Logging`; `config/config.exs` `filter_parameters`; Pumble/webhook `log: false` | `test/observability/logging_test.exs` (OAuth, callback, Pumble action, HTTP action, approval, uncertainty, exception captures; JSON schema; diagnostic hashes); `test/security/web_security_test.exs` planted authorization code; `test/security/release_gate_test.exs` (unique canary absent from logs and diagnostic map) | P14-T01 this change; P15-T05 | Logs remain sensitive operational data. Retention and access live in the drain (`docs/operations/logging.md`). Diagnostic mode emits hashes/lengths only, never bodies. |
| Stored content injection | LiveView | HEEx escaping, CSP `script-src 'self'`, no `Phoenix.HTML.raw` | `PumbleAutomationWeb.Plugs.SecurityHeaders`; LiveView templates | `test/security/web_security_test.exs` (`the public document has no inline script`, `the router CSP matches the canonical policy`); `test/security/threat_closure_test.exs` (no `Phoenix.HTML.raw` in templates); `test/pumble_automation_web/live/execution_live_test.exs` planted payload off the page | P12-T08 `e57532d`; P13-T05 `7ed72d4`; re-run in P13-T07 | CSP allows `'unsafe-inline'` for **style** only (LiveView show/hide). Scripts stay `'self'`. `frame-ancestors 'self'` (no Pumble iframe in v1). |
| Admin abuse | Support tools | Finite owner catalogue; tenant-scoped; audited; no SQL/eval/global admin | `PumbleAutomation.Operations` (`lib/pumble_automation/operations.ex`) | `test/pumble_automation/audit/audit_test.exs` (`support operations are a finite named catalogue`, `an editor cannot run support operations`, `another workspace's job is not found`, `an unsafe requeue writes no audit row`); `test/pumble_automation_web/live/audit_live/index_test.exs` (`an owner sees the support panel; a viewer does not`); `test/security/release_gate_test.exs` (`production debug surfaces are not compiled into the router`) | P13-T06 `2f4f51f`; re-run in P13-T07; P15-T05 | Diagnostic ZIP packaging is **P14-T06**. Export preview is sanitized now. No super-admin UI exists. |
| Dependency compromise | Build | Lockfile, Hex audit, minimal deps, documented overrides | `mix.lock`; `scripts/verify.sh` gate 7; ADR-0011 | `mix hex.audit` (no retired or advisory packages); `test/security/threat_closure_test.exs` (hackney 4.0.3 pin); CI gitleaks | P13-T07 this review; `mix hex.audit` clean; P15-T05 re-runs the audit | tzdata 1.1.4 pulls hackney for unused IANA downloads; hackney is overridden to 4.0.3 (ADR-0011). Image/container scans are P16/P18-T03. P18-T03 re-runs the audit. |

## Data classification (unchanged)

See `docs/contract/threat_model.md` §2. Invariants still hold:

- no raw credential in logs, workflow JSON, execution history, metrics, or ordinary errors;
- tenant ID alone is never authorization;
- debug routes are absent in production (`config/prod.exs` `:dev_routes false`).
